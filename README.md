# Sky Step Rush

간단한 무한의계단 느낌의 웹게임입니다.

## 실행

```bash
npm start
```

브라우저에서 `http://localhost:4173` 를 열면 됩니다.

## 테스트

```bash
npm test
```

## 조작

- `←` / `A`: 왼쪽 계단으로 이동
- `→` / `D`: 오른쪽 계단으로 이동
- `↑` / `Space`: 직진
- `R`: 재시작
